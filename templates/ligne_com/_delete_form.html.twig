<form method="post" action="{{ path('app_ligne_com_delete', {'id': ligne_com.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_com.id) }}">
    <button class="btn">Delete</button>
</form>
